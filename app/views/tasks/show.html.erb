<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= check_box("completed",@task.completed,{checked: @task.completed} )%><%= @task.completed ? "This task is completed" : "Yet to complete" %></p>

<%= link_to "Back to home page", tasks_path %>
